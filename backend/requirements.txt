# Runtime principal
# Instalar con: pip install -r requirements.txt

# Framework y servidor HTTP
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ORM / base de datos
sqlmodel>=0.0.16
psycopg[binary]>=3.1.0
alembic>=1.13.0

# Zona horaria IANA (Windows no trae base tz del sistema)
tzdata>=2024.1

# Verificación de JWT de Supabase Auth (opcional, ver docs/seguridad.md)
pyjwt>=2.8.0

# Parseo de archivos
pandas>=2.0.0
openpyxl>=3.1.0
pypdf>=3.0.0
pdfplumber>=0.11.0

# Integracion OpenAI (OCR por API, clasificacion asistida)
openai>=2.36.0

# Agente de RRHH (Gemini via Google Generative AI)
google-generativeai>=0.8.0
Pillow>=10.0.0

# Gmail API
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0

# Rate limiting
slowapi>=0.1.9

# Cifrado simétrico (token OAuth Gmail en DB)
cryptography>=42.0.0

# Scheduler
apscheduler>=3.10.0

# Supabase Storage (CVs en la nube)
supabase>=2.0.0

# Linting, formato y tests (dev)
ruff>=0.4.0
pytest>=8.0.0
